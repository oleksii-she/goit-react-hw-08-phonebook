import { TitleLog, BoxLog } from './home.styled';

export const HomeComponents = () => {
  return (
    <BoxLog>
      <TitleLog>
        Please log in or register in the application to work with it
      </TitleLog>
    </BoxLog>
  );
};
